# TODO List

* Make url of booking api configurable by settings for ng app
* Provide a list of prerequisites of the local machine (docker, mvn, ng, npm)
* Configure spring-boot api so that it can launch with database connection
* Implement consistent naming in the api (booking vs room)
* Use more distinguished paths for ui and api (80, 8080 can be confused easily)